<% title "Budgets : edit" %>

<div class="row">
    <div class="span6">
        <%= render 'form' %>
    </div>
</div>
<p><%= link_to '<i class="icon-chevron-left"></i> budgets list'.html_safe, budgets_path, :title => 'budgets list' %></p>