<%= form_for(@expense) do |f| %>
  <% if @expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@expense.errors.count, "error") %> prohibited this expense from being saved:</h2>

      <ul>
      <% @expense.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :userId, {:value => session[:user][:id]} %>
  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description, :placeholder => "expense description", :class => 'input-xxlarge' %>
  </div>
  <div class="field input-append">
    <%= f.label :amount %>
    <%= f.text_field :amount, :placeholder => "expense amount" %>
    <span class="add-on">zł</span>
  </div>
  <div class="field">
    <%= f.label :date %>
    <%= f.text_field :date, :placeholder => "expense date", :class => 'input-small' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
