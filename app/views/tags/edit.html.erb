<% title "Tags" %>

<div class="row">
	<div class="span6">
		<%= render 'form' %>
	</div>
</div>

<p><%= link_to '<i class="icon-chevron-left"></i> all tags'.html_safe, tags_path, :title => 'all tags' %></p>
